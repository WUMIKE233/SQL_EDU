# 复习课件抽取笔记

`复习课件1.pptx` 共 33 页，可通过 `python-pptx` 抽取文本。已纳入题库的重点包括：

1. 幻灯片 3-7：数据、数据库、DBMS、DBS、数据库系统特点、三级模式、二级映像。
2. 幻灯片 11-14：关系、元组、属性、主码、外码、完整性约束、关系代数。
3. 幻灯片 23-26：SQL 特点、DDL/DML/DCL、SELECT、ORDER BY、GROUP BY/HAVING、INSERT/UPDATE/DELETE。
4. 幻灯片 28：为 SC 表添加学生和课程外键。
5. 幻灯片 31-33：视图概念、作用、创建、查询、删除和可更新限制。

抽取时 Windows 控制台默认编码无法输出关系代数符号，已通过 `PYTHONIOENCODING=utf-8` 处理。
