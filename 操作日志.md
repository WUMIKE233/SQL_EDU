# SQL EDU 操作日志

## 2026-06-13 14:52:07 +08:00

### 本次目标

- 基于 `数据库PTA习题.pdf` 与 `复习课件1.pptx` 构建本地答题库。
- 实现可在本地运行的复习前端。

### 已完成

- 阅读仓库现状：已有 `book/` 教材章节、`examples/` 示例 SQL、双语 `README.md`。
- 抽取材料：
  - `复习课件1.pptx` 共 33 页，使用 `python-pptx` 抽取文本。
  - `数据库PTA习题.pdf` 共 7 页，正文为图片题，使用 `pypdf` 提取嵌入图片并人工核读清晰题目。
- 新增题库：
  - `src/data/questionBank.ts`，共 54 题。
  - 覆盖判断题、单选题、填空题、SQL 题、简答题。
  - 覆盖 SQL 语言、关系模型、规范化、事务恢复、存储过程与触发器、视图与权限等专题。
- 新增本地复习前端：
  - React + Vite + TypeScript。
  - 支持专题筛选、题型筛选、来源筛选、搜索、随机出题、显示答案、收藏、错题/掌握状态、本地进度保存。
  - 保存设计概念图到 `docs/ui-concept.png`。
- 新增材料说明：
  - `materials/answer-bank.md`
  - `materials/pptx-extraction-notes.md`
  - `materials/pdf-extraction-notes.md`
- 更新 `.gitignore`：
  - 忽略 `node_modules/`、`dist/`、`.vite/`、`tmp/`、`output/`。

### 验证

- `npm install` 完成依赖安装。
- `npm run build` 通过，Vite 生产包成功生成。
- 本地开发服务启动于 `http://127.0.0.1:5188`。
- Playwright 验证通过：
  - 首页标题为 `SQL EDU 复习题库`。
  - 全部题目计数为 `题目 1 / 54`。
  - 规范化专题筛选后计数为 `题目 1 / 9`。
  - 已验证随机出题、显示答案、已掌握、专题筛选和移动端布局。

### 备注

- `tmp/pdf_images/` 与 `output/playwright/` 为本次抽取和验证的本地临时证据；因项目要求删除操作需人工审核，未执行删除，仅通过 `.gitignore` 排除版本控制。
- 5173 端口被其他本地服务占用，因此 SQL EDU 前端改用 5188 端口。

## 2026-06-13 15:25:38 +08:00

### 本次目标

- 根据两张微信截图继续补充本地复习题库。

### 已完成

- 读取新增截图：
  - `第3周` 截图：数据库基础、主键/候选码、三级模式、DDL、索引、建表约束。
  - `第6周` 截图：子查询、连接、BETWEEN、LIKE、INSERT/UPDATE/DELETE、聚合、SQL 查询。
- 将两张长图裁剪到 `tmp/new_question_images/` 后逐段核读。
- 更新 `src/data/questionBank.ts`：
  - 新增第3周题目 17 题。
  - 新增第6周题目 20 题。
  - 题库总数从 54 题增加到 91 题。
- 更新 `materials/answer-bank.md`，同步新增来源与题目总数。

### 验证

- `npm run build` 通过。
- Playwright 验证本地前端 `http://127.0.0.1:5188`：
  - 首页标题为 `SQL EDU 复习题库`。
  - 题库总数显示 `91`。
  - 全部题目计数为 `题目 1 / 91`。
  - 搜索 `CREATE TABLE` 命中 `5` 题。
  - 搜索 `LIKE` 命中 `2` 题。

### 备注

- 未执行删除操作。
- 本次截图裁剪与验证截图保留在 `tmp/`、`output/`，均已通过 `.gitignore` 排除版本控制。

## 2026-06-13 20:49:18 +08:00

### 本次目标

- 将本地复习前端发布到公网，使其可以通过 GitHub Pages 实时访问。

### 已完成

- 新增 `.github/workflows/deploy-pages.yml`：
  - 推送到 `main` 或手动触发时自动安装依赖、构建 Vite 静态站点并部署到 GitHub Pages。
- 更新 `vite.config.ts`：
  - 本地运行继续使用 `/`。
  - GitHub Actions 构建时使用 `/SQL_EDU/` 作为静态资源 base，适配仓库 Pages 子路径。
- 更新双语 `README.md`：
  - 补充本地复习前端说明、运行命令与公网访问地址。
- 保留现有题库与本地复习功能，未执行删除操作。

### 验证

- `npm run build` 通过。
- `GITHUB_ACTIONS=true npm run build` 通过，验证 GitHub Pages 子路径构建配置可用。

### 备注

- 计划公网地址：`https://wumike233.github.io/SQL_EDU/`。
- 下一步提交并推送到 `main`，随后启用/触发 GitHub Pages 工作流并验证公网 HTTP 访问。
